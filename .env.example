# TaxOS local configuration — copy to .env (gitignored). Defaults match docker-compose.yml.
TAXOS_ENV=local
TAXOS_RELEASE=dev
TAXOS_DATABASE__DSN=postgresql+asyncpg://taxos:taxos@localhost:5432/taxos
TAXOS_REDIS__URL=redis://localhost:6379/0
