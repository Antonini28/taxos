"""Audit module — append-only, hash-chained (ADR-009).

Import submodules directly (`taxos_core.audit.models`, `.verify`) — see the note in
`shared.persistence.__init__` on keeping the import graph acyclic.
"""
